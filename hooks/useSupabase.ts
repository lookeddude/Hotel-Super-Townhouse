/**
 * hooks/useSupabase.ts
 * ─────────────────────
 * Re-export useSupabase hook for clean import paths.
 *
 * Usage:
 *   import { useSupabase } from '@/hooks/useSupabase';
 */
export { useSupabase } from '@/providers/SupabaseProvider';
